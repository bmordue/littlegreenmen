<%= form_for(@game_action) do |f| %>
  <% if @game_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_action.errors.count, "error") %> prohibited this game_action from being saved:</h2>

      <ul>
      <% @game_action.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :turn_id %><br />
    <%= f.select :turn_id, Turn.find(:all)  %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
